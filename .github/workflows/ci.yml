name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ruff:
    name: ruff (lint + format)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.8.15"

      # Ruff runs STANDALONE (uvx): linting must not require resolving any dependency
      # stack. Config lives in pyproject [tool.ruff]; pin matches 3lc-compute-plugin-sdk.
      - name: Ruff lint
        run: uvx --from 'ruff>=0.15,<0.16' ruff check .

      - name: Ruff format check
        run: uvx --from 'ruff>=0.15,<0.16' ruff format --check .
